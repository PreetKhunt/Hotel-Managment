{
  "name": "hospitality-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "kill-port 3001 5000 && concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "wait-on tcp:5000 && cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "kill-port": "^2.0.1",
    "wait-on": "^9.0.10"
  }
}
